# URL base del backend RAG (servicio Docker separado de este frontend).
# En local, apunta a tu contenedor; en Vercel, configúralo como variable
# de entorno del proyecto apuntando a la URL pública de tu backend desplegado.
VITE_RAG_API_URL=http://localhost:8000
